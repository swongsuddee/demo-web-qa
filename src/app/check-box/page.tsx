import { Metadata } from "next";

export const metadata: Metadata = {
    title: '4. Check box',
};

const CheckBox = (() => {
    return (
        <div>

        </div>
    )
});

export default CheckBox;